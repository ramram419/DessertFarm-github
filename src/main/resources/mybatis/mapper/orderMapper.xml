<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="order">
	<select id="selectOrderList" parameterType="String" resultType="orderDTO">
		SELECT p.product_name, o.product_quan, o.product_price, o.order_date 
		FROM product p, product_image i, order_detail o 
		WHERE o.client_id = #{id}
		ORDER BY o.order_date DESC  
	</select>
	
	<select id="getTotalOrder" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM order WHERE client_id = #{id}
	</select>
	
	<insert id="insertOrderList" parameterType="orderDTO">
		
	</insert>
</mapper>