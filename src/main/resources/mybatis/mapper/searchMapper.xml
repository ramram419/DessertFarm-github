<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="search">
	<select id="searchProduct" parameterType="hashmap" resultType="searchProductDTO">
		select m.manager_name,p.product_name, p.category,p.product_price,i.pro_img_server from product p, manager m, product_image i
      	where p.product_id = i.product_id and p.manager_id = m.manager_id and p.product_name LIKE CONCAT('%', #{keyword}, '%') OR p.category LIKE CONCAT('%', #{keyword}, '%') OR p.product_price  LIKE CONCAT('%', #{keyword}, '%') 
      	and i.pro_img_server like '%thumb____'
      	limit #{findingCount} offset #{dbLimitStart}
	</select>
	
	<select id="getTotalSearchProduct" resultType="int">
		select count(*) from product where product_id LIKE CONCAT('%', #{keyword},'%') OR product_name LIKE CONCAT('%', #{keyword}, '%') OR category LIKE CONCAT('%', #{keyword}, '%') OR product_price LIKE CONCAT('%', #{keyword}, '%')
	</select>
</mapper>